spring:
  h2:
    console:
      enabled: true
    sql:
      init:
        mode: always
    datasource:
      url:

      driver-class-name: org.h2.Driver
      username: sa
      password: password

ru:
  yandex:
    practicum:
      filmorate:
        settings:
          greeting: 'LOCAL'